export function addScore(game, delta = 1) {const scores = JSON.parse(localStorage.getItem("scores") || "{}");scores[game] = (scores[game] || 0) + delta;localStorage.setItem("scores", JSON.stringify(scores));}export function getScores() {return JSON.parse(localStorage.getItem("scores") || "{}");}export function addHistory(game, result) {const list = JSON.parse(localStorage.getItem("history") || "[]");list.unshift({ game, result, time: new Date().toLocaleString() });localStorage.setItem("history", JSON.stringify(list.slice(0, 100)));}export function getHistory() {return JSON.parse(localStorage.getItem("history") || "[]");}